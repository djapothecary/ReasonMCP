namespace ReasonMPC.Agents
{
    /// <summary>
    /// This agent is used to evalute current information (knowledge bases) and generate prompts
    /// Tank (The Operator) evaluates the context and loads the exact instructions/prompt.
    /// </summary>
    public class TankAgent
    {

    }
}